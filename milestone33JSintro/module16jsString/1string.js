const country = 'Bangladesh';
const division = "noakhali";
const district = `Bramman baria`;
const thana = new String('Demra'); //creates object of string

console.log(typeof thana); 

const capital = 'dhaka';

console.log(capital.length);//returns the length
capital[0] = 'F';//will not work
//because string is immutable
